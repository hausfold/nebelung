# CLAUDE.md

@AGENTS.md

Claude-only wiring: `.claude/settings.json` runs `.agents/setup.sh` on `SessionStart`; the `WorktreeCreate`/`WorktreeRemove` hooks in `~/.claude/settings.json` are haus's. Map: [`.agents/README.md`](./.agents/README.md).
